name: favorite_places_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.11.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2
  image_picker: ^0.8.4
  path_provider: ^2.0.9
  path: ^1.7.0
  sqflite: ^2.0.2
  location: ^4.3.0
  google_maps_flutter: ^2.1.2
  http: ^0.13.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  